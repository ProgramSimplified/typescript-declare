import multiply, { add } from 'my-lib';
const mulRes = multiply(1, 2);
const res = add(1, 2);
const a = 'abc';
const b = {
    x: 1,
    y: '1'
};
const extendObj = {
    m: null,
    n: undefined
};
console.log(`恭喜你㊗️, 完成运行! 结果为 ${res}`);
